"use client";

import React from "react";

const AuthLogo = () => (
  <div className="flex flex-col items-center mb-8 select-none">
    <div className="flex items-center justify-center h-40 mb-4 rounded-full bg-primary/10">
      <img src="/logo-st.svg" alt="Logo" className="h-40" />
    </div>
  </div>
);

export default AuthLogo;
